原项目模板自带发版,在此基础上，本项目对部分流程进行了自定义更改。

1. 将 python 等依赖一并打包.
2. 将 GUI 仓库自带默认图标更换为自定义图标，包括对 Assets 文件中的 logo 文件替换，以及对二进制文件标志替换。目前尚未实现任务栏图标替换。(如果后续 GUI 仓库更新了对自定义图标更好的支持,此部分可移除。)
3. 增加了替换 Win32 控制器 SendMessage 的内容
